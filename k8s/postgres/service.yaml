apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: ecoprompt
  labels:
    app: postgres
    app.kubernetes.io/part-of: ecoprompt
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
