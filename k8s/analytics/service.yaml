apiVersion: v1
kind: Service
metadata:
  name: analytics-svc
  namespace: ecoprompt
  labels:
    app: analytics
    app.kubernetes.io/part-of: ecoprompt
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
    - port: 5002
      targetPort: 5002
      protocol: TCP
