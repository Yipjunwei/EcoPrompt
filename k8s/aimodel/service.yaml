apiVersion: v1
kind: Service
metadata:
  name: aimodel-svc
  namespace: ecoprompt
  labels:
    app: aimodel
    app.kubernetes.io/part-of: ecoprompt
spec:
  type: ClusterIP
  selector:
    app: aimodel
  ports:
    - port: 5003
      targetPort: 5003
      protocol: TCP
